# Map Editor

Status: placeholder.

This tool is reserved for ignition map editing.

Deferred until:

- Timing map format is defined.
- Firmware accepts validated calibration data.
- Bench tests prove timing output against known profiles.

The first map format should target small single-cylinder 2-stroke engines under 200cc without locking to one model.
