# Logging Module

Purpose: record bench data needed to debug timing, charger behavior, and faults.

Minimum future log fields:

```text
time_us,rpm,advance_deg,vbat,vcap,charge_us,event,error
```

No implementation exists yet.
