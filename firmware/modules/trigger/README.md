# Trigger Module

Purpose: send a controlled trigger command to the CDI Power Board.

Contracts:

- Default output state is inactive.
- Trigger is blocked unless safety module allows it.
- Trigger pulse timing is measurable on bench.
- Reset or watchdog event returns output to inactive state.

No implementation exists yet.
